{
  "name": "checkit-project",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "dev": "http-server -c-1",
    "build": "mkdir -p dist && cp -R ./* dist/ && cp -R ./.idx dist/.idx && cp -R ./assets dist/assets"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
